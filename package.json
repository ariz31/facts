{
  "name": "facts-learning-cards",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:data": "node scripts/validate-data.mjs",
    "check": "npm run check:data && npm test"
  }
}
